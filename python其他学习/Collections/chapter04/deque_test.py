from collections import deque

# name_list = ["faker", "hong"]
# name_list.pop(0)
# print(name_list)

user_list = deque(["faker3", "faker2", "faker3"])
a = [1,2,5,3]
a.sort()
b = s
print(b)

# deque是线程安全的，GIL是线程安全的，list不是线程安全的